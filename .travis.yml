sudo: false

language: node_js

notifications:
  email: false

node_js:
  - stable

cache:
  directories:
    - node_modules

before_script:
  - npm install

script:
  - npm run test

branches:
  only:
  - master
